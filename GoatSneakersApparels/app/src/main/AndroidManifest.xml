<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoatSneakersApparels"
        tools:targetApi="31">

        <!-- MainActivity declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity declaration -->
<!--        <activity android:name=".SignInActivity" android:exported="true" />-->
        <activity android:name=".SearchEngineActivity" />
        <activity android:name=".MyLoginActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OffersProfActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".OrdersProfActivity" />
        <activity android:name=".OwnsProfActivity" />
        <activity android:name=".WantsProfActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".StyleViewActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".StyleActivity" />
        <activity android:name=".SellActivity" />
        <activity android:name=".AddtocartActivity"/>
        <activity android:name=".SignupActivity" android:exported="true" />
    </application>

</manifest>
